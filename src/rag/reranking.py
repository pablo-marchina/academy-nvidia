"""Scaffold for reranking retrieved context before recommendation generation."""


def rerank_context(chunks: list[str]) -> list[str]:
    """Placeholder reranker that preserves input order for now."""

    return list(chunks)
